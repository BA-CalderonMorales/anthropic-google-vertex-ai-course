# notebooks/

This directory is for Google Colab-first implementation notebooks aligned to the core course sections.

Guidelines:
- Keep one primary notebook per section (`workbook.ipynb`).
- Prefer reproducible cells over ad-hoc snippets.
- Use `config/` for shared setup details (environment variables template, constants, helper snippets).
- Keep this repo focused on learning artifacts, not exam answers.
